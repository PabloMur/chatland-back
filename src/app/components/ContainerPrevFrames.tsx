const CustomPrevFrames = ({ children }: any) => {
  return (
    <div className="h-screen w-full bg-indigo-900 grid place-items-center sm:flex sm:flex-row">
      {children}
    </div>
  );
};
export default CustomPrevFrames;
